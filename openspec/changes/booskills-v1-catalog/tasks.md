## 1. Repo Scaffolding

- [x] 1.1 Run `git init` at /home/samkintop/opt/booskills and create .gitignore excluding forks/, .DS_Store, node_modules/, *.swp, and .idea/. Verify: `git status` shows .gitignore as the only untracked file in the repo root (excluding forks/).
- [x] 1.2 Create directory structure: skills/, agents/, agents/codex/, scripts/, .claude-plugin/. Verify: all five directories exist and are empty.
- [x] 1.3 Create .claude-plugin/marketplace.json following the pattern in forks/han/.claude-plugin/marketplace.json, with name "booskills", relative source path, and no remote GitHub URLs. Verify: `python3 -c "import json; json.load(open('.claude-plugin/marketplace.json'))"` succeeds and contains a plugins array.
- [x] 1.4 Create .claude-plugin/plugin.json following the pattern in forks/han/han.core/.claude-plugin/plugin.json, with exactly the fields name "booskills", description, and version "1.0.0". Do NOT add skills/agents path keys; Claude Code discovers skills/ and agents/ at their default plugin-root locations, and han's manifest uses only these three fields. Verify: `python3 -c "import json; json.load(open('.claude-plugin/plugin.json'))"` succeeds and contains name, description, and version keys only.

## 2. Reference Skill: reviewing-code

- [x] 2.1 Create skills/reviewing-code/SKILL.md following SKILL_GUIDELINES.md Section 10 skeleton. Write frontmatter (name: reviewing-code, description under 1024 chars with boundary clauses naming auditing-code-quality and investigating-failures, metadata.version: "1.0"). Verify: `wc -l skills/reviewing-code/SKILL.md` is at most 500; description is at most 1024 chars.
- [x] 2.2 Write the Size section (default small, sizing from files/subsystems/surfaces touched, accept $size override). Verify: the Size section header exists and mentions "small" as default.
- [x] 2.3 Write the Process section: size classification, always dispatch junior-developer + adversarial-security-analyst, conditional roster by diff scope (test-engineer, edge-case-explorer, structural-analyst, behavioral-analyst, concurrency-analyst), classify findings as blocking/advisory/nit, YAGNI gate on advisory, output review report with verdict. Verify: Process names junior-developer and adversarial-security-analyst as always-dispatched; mentions classification (blocking/advisory/nit) and verdict (approve/approve-with-changes/block).
- [x] 2.4 Write the What NOT to do section (no fixing, no findings on unchanged code, no style nits outside the diff). Write the Gotchas section (evidence rule, sizing, no-commit, no em dashes, Pi degradation for agent dispatch). Verify: What NOT to do contains at least the three specified prohibitions; Gotchas contains the Pi subagent note.
- [x] 2.5 Write the Output format section with review report template ending in "Claims I did not verify". Write the Failure modes section (empty diff, unresolvable merge conflict, missing repo). Verify: Output format ends with "Claims I did not verify"; Failure modes lists at least three failure cases.
- [x] 2.6 Verify the complete SKILL.md: no em dashes (U+2014), frontmatter uses only agentskills.io superset fields, all mandatory sections present (Size, Process, What NOT to do, Gotchas, Output format, Failure modes). Verify: `grep -c $'\xe2\x80\x94' skills/reviewing-code/SKILL.md` returns 0; `wc -l` is at most 500.

## 3. Agent Roster Port

- [x] 3.1 Port adversarial-security-analyst from forks/han/han.core/agents/adversarial-security-analyst.md to agents/adversarial-security-analyst.md: preserve frontmatter (name, description, tools, model), persona, domain vocabulary, anti-patterns, protocol headers, and output format (E# numbered findings with file:line); strip Han-internal path references (plugins/han/) and Han dispatch syntax (/skill-name). Verify: `wc -l agents/adversarial-security-analyst.md` is at most 500; `grep -c "plugins/han/" agents/adversarial-security-analyst.md` returns 0; description is under 1024 chars.
- [x] 3.2 Port adversarial-validator, evidence-based-investigator, and junior-developer following the same process as task 3.1. Verify: all three files exist in agents/ with correct frontmatter, no Han-internal references, descriptions under 1024 chars.
- [x] 3.3 Port structural-analyst, behavioral-analyst, concurrency-analyst, and risk-analyst following the same process. Verify: all four files exist in agents/ with correct frontmatter, no Han-internal references.
- [x] 3.4 Port software-architect, test-engineer, edge-case-explorer, and user-experience-designer following the same process. Verify: all four files exist in agents/ with correct frontmatter, no Han-internal references.
- [x] 3.5 Verify agent roster completeness: exactly 12 .md files in agents/, one per the SKILL_CATALOG_SPEC.md roster. Verify: `ls agents/*.md | wc -l` returns 12; each filename matches a catalog entry.
- [x] 3.6 Create agents/codex/adversarial-security-analyst.toml: name, description (under 1024 chars), developer_instructions (full markdown body from the .md file), model. Verify: TOML parses without error (`python3 -c "import tomllib; tomllib.load(open('agents/codex/adversarial-security-analyst.toml'))"`).
- [x] 3.7 Create the remaining 11 TOML files in agents/codex/ by converting each corresponding .md file. Verify: `ls agents/codex/*.toml | wc -l` returns 12; each TOML file parses without error; each developer_instructions field contains the full agent body.

## 4. investigating-failures Skill

- [x] 4.1 Create skills/investigating-failures/SKILL.md following Section 10 skeleton. Include description with boundary clauses (Do NOT use for reviewing-code or mapping-project-context). Write Process: reproduce/characterize first, dispatch evidence-based-investigator, conditional specialists, proposed fix, dispatch adversarial-validator. Verify: description names reviewing-code and mapping-project-context as boundaries; Process names evidence-based-investigator as first-dispatched.
- [x] 4.2 Write What NOT to do (never apply fix, never conclude from single web source, passing test not evidence bug absent), Gotchas (evidence rule, Pi degradation, sizing, no-commit, no em dashes), Output format (E# evidence items, root cause, proposed fix, V# validation findings, reproduction command, Claims I did not verify), Failure modes (irreproducible failure, ambiguous symptoms, no codebase access). Verify: all mandatory sections present; Output format ends with "Claims I did not verify"; `wc -l` at most 500; no em dashes.

## 5. mapping-project-context Skill

- [x] 5.1 Create skills/mapping-project-context/SKILL.md with description naming analyzing-architecture and investigating-failures as boundaries. Process: enumerate from disk, trace entry points from code not docs, read existing context docs last and diff against observed reality, size-controlled depth (small = single-pass, medium/large = dispatch structural-analyst). Target output: context map (structure, services, data stores, deps, conventions, build/deploy commands) plus doc-drift section. Verify: description names boundaries; What NOT to do states no recommendations; Gotchas note roadmap/changelog files are never sole evidence; at most 500 lines; no em dashes.

## 6. planning-changes + implementing-changes Skills (pair)

- [x] 6.1 Create skills/planning-changes/SKILL.md. Description: produces a validated OpenSpec change folder; Do NOT use for fuzzy ideas (use refining-ideas) or existing plan execution (use implementing-changes). Hard contract: only output is the OpenSpec change folder, never application code. Process: precondition check (openspec/ exists), consume requirements, generate change folder, YAGNI gate, validate with openspec validate, dispatch adversarial-validator + junior-developer. Verify: Hard contract section states "only output is the OpenSpec change folder, never application code"; Process includes `openspec validate` step; description names refining-ideas and implementing-changes as boundaries; at most 500 lines.
- [x] 6.2 Create skills/implementing-changes/SKILL.md. Description: implements an existing validated OpenSpec change folder task-by-task; Do NOT use without a change folder (use planning-changes) or for ad-hoc fixes. Hard contract: input is a change-id; scope is tasks.md only. Process: read proposal, design, tasks; refuse if validation errors; execute tasks in order; verify each; check box in tasks.md; report per-task status + diff stat. Deviation rule: stop at design conflict, write discrepancy into design.md, report. Verify: Hard contract states "scope is tasks.md, nothing else"; What NOT to do includes no work outside tasks.md, no "while I'm here" refactors, no committing, no archiving; at most 500 lines.

## 7. refining-ideas Skill

- [x] 7.1 Create skills/refining-ideas/SKILL.md. Description: turns rough ideas into buildable requirements sketches through targeted questions; Do NOT use when requirements are clear (go straight to planning-changes). Process: read idea, infer from context, ask max 3 questions per round, restate sharpened idea, stop when sketch is hand-off-able to planning-changes. Output: requirements sketch (problem statement, actors, success criteria, in-scope, out-of-scope, open questions, backend/frontend split). What NOT to do: never propose design, never exceed 3 questions per round, never ask anything answerable from repo, never pad with filler. Verify: Process states max 3 questions per round; What NOT to do lists the four prohibitions; at most 500 lines; no em dashes.

## 8. auditing-code-quality Skill

- [x] 8.1 Create skills/auditing-code-quality/SKILL.md. Description: scans for AI slop, refactor candidates, optimization opportunities with scored remediation backlog; Do NOT use for reviewing a specific diff (use reviewing-code) or diagnosing failure (use investigating-failures). Tree-scoped (no diff required). Process: size by tree scope, run mechanical detectors first (scripts/ per stack: lint, dead-code, duplication), then agent pass (structural-analyst for refactor candidates, behavioral-analyst for logic quality), score findings impact x effort, YAGNI gate optimizations without measured pain. Output: backlog table (finding, category, file:line, impact, effort, remediation), Deferred section for speculative optimizations. Verify: Process names mechanical detectors first; YAGNI gate on speculative optimizations; What NOT to do includes no fixing during audit, no "rewrite it all" findings; at most 500 lines.

## 9. researching Skill

- [x] 9.1 Create skills/researching/SKILL.md. Description: researches technical questions across web and local sources with sourced recommendation and explicit evidence status; Do NOT use for questions answerable from codebase alone (use mapping-project-context). Process: define decision research serves, gather wide-then-deep preferring primary sources, tag every claim with trust class (codebase/web/provided) and corroboration, record conflicting sources both ways, recommendation only from claims that pass corroboration gate. Output: decision statement, recommendation, claims table, No evidence yet section, Claims I did not verify. What NOT to do: never let LLM-generated explanation count as source, never silently resolve conflict. Verify: Process instructs trust-class tagging; What NOT to do names the two prohibitions; at most 500 lines; no em dashes.

## 10. analyzing-architecture Skill

- [x] 10.1 Create skills/analyzing-architecture/SKILL.md. Description: evaluates codebase architecture with evidence-backed recommendations; Do NOT use for neutral context maps (use mapping-project-context) or single-diff reviews (use reviewing-code). Prerequisite: current context map exists (or run mapping-project-context first). Process: dispatch structural-analyst, behavioral-analyst, concurrency-analyst, risk-analyst in parallel, then software-architect synthesizes, YAGNI gate every recommendation. Output: findings per analyst lens, synthesized recommendations with forcing functions, Deferred section, cross-service concerns flagged out-of-scope. Verify: Process names all four analyst agents and the software-architect synthesizer; description names boundaries; at most 500 lines.

## 11. critiquing-frontend Skill (port from impeccable)

- [x] 11.1 Read forks/impeccable/skill/SKILL.src.md completely. Extract the substantive design guidance (color, typography, layout, motion, interaction rules) into references/ within the skill directory, preserving the content verbatim. Verify: references/ subdirectory exists within skills/critiquing-frontend/ with the ported content.
- [x] 11.2 Create skills/critiquing-frontend/SKILL.md from the impeccable source: strip all provider tags (<codex>, <gemini>), strip all placeholder syntax ({{variable}}), wrap in Section 10 skeleton. Add frontmatter (name: critiquing-frontend, description with boundary clauses naming reviewing-code for backend review), Size (default small, dispatch user-experience-designer at medium+), Process (apply design guidance from references/, dispatch user-experience-designer), What NOT to do (not for backend code review, not for building new UI), Gotchas (BooLab primitive rule, anti-cream/serif default-aesthetic for dashboards, evidence rule, Pi degradation, no-commit, no em dashes), Output format (findings grouped by severity: broken/inconsistent/polish, each citing component file:line, then Claims I did not verify), Failure modes. Verify: no <codex> or <gemini> tags; no {{variable}}; all Section 10 sections present; BooLab Gotcha present; at most 500 lines; description under 1024 chars.

## 12. Local Installer Script

- [x] 12.1 Create scripts/install.sh. The script: (a) creates parent directories if they don't exist (~/.agents/skills/, ~/.codex/agents/, ~/.config/opencode/agents/), (b) creates symlinks: ~/.agents/skills/booskills -> repo/skills/, each ~/.codex/agents/<name>.toml -> repo/agents/codex/<name>.toml, each ~/.config/opencode/agents/<name>.md -> repo/agents/<name>.md, (c) skips existing correct symlinks, (d) reports conflicts and exits non-zero for wrong-target symlinks, (e) falls back to copy if symlinks fail, (f) verifies each link resolves, (g) prints summary. Accepts optional --copy flag to force copy mode. Contains NO "npx skills add". Verify: `bash -n scripts/install.sh` succeeds (syntax check); `grep -c "npx skills add" scripts/install.sh` returns 0.
- [x] 12.2 Make scripts/install.sh executable. Manually test on this machine: run the script, verify symlinks exist at ~/.agents/skills/booskills, ~/.codex/agents/adversarial-security-analyst.toml, and ~/.config/opencode/agents/adversarial-security-analyst.md. Check that each resolves. Verify: `ls -la ~/.agents/skills/booskills` shows symlink to /home/samkintop/opt/booskills/skills/; at least one ~/.codex/agents/*.toml and one ~/.config/opencode/agents/*.md resolve.

## 13. Final Verification

- [x] 13.1 Count skill directories: `ls skills/*/SKILL.md | wc -l` returns 10. Verify: the count is exactly 10.
- [x] 13.2 Count agent .md files: `ls agents/*.md | wc -l` returns 12. Count agent .toml files: `ls agents/codex/*.toml | wc -l` returns 12. Verify: both counts are 12.
- [x] 13.3 Verify each SKILL.md is at most 500 lines: `for f in skills/*/SKILL.md; do lines=$(wc -l < "$f"); if [ "$lines" -gt 500 ]; then echo "FAIL: $f has $lines lines"; fi; done`. Verify: no FAIL output.
- [x] 13.4 Verify each SKILL.md description is at most 1024 chars: `for f in skills/*/SKILL.md; do desc=$(python3 -c "import yaml; print(yaml.safe_load(open('$f'))['description'])" 2>/dev/null); if [ ${#desc} -gt 1024 ]; then echo "FAIL: $f description is ${#desc} chars"; fi; done`. Verify: no FAIL output.
- [x] 13.5 Verify no em dashes in any SKILL.md or agent .md: `grep -rl $'\xe2\x80\x94' skills/ agents/`. Verify: the command returns no output.
- [x] 13.6 Verify all mandatory sections in each SKILL.md (Size, Process, What NOT to do, Gotchas, Output format, Failure modes): `for f in skills/*/SKILL.md; do for section in "Size" "Process" "What NOT to do" "Gotchas" "Output format" "Failure modes"; do grep -qi "## $section" "$f" || echo "MISSING: $section in $f"; done; done`. Verify: no MISSING output.
- [x] 13.7 Verify SKILL_GUIDELINES.md and SKILL_CATALOG_SPEC.md remain at repo root: `ls SKILL_GUIDELINES.md SKILL_CATALOG_SPEC.md`. Verify: both files exist.
- [x] 13.8 Run `openspec validate booskills-v1-catalog` from the repo root. Verify: validation passes with no errors.