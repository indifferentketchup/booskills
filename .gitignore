# Dependencies / build output
node_modules/
.next/
out/
dist/

# Local tooling caches / session state (never ship)
.aislop/
.codesight/
.commandcode/
.boocontext/
.ast-cache/
.idea/
__pycache__/
*.pyc
*.swp
.DS_Store

# Vendored upstream reference, not part of the catalog
forks/

# Backups and logs
*.bak
*.ui-bak
*.log

# Local per-service env (e.g. tailnet bind IPs) — never commit
.env
